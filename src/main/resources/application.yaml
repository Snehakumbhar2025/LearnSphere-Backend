spring:
  application:
    name: LearnSphere-Backend

  datasource:
    url: jdbc:mysql://localhost:3306/learnsphere_db
    username: spring
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

        logging:
          level:
            org.springframework.security: DEBUG